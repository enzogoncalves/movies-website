import styled from "styled-components";

export const NavBar = styled.div`
  grid-area: lateral-nav-bar;
  padding: 2rem;
`

export const List = styled.ul`
  display: flex;
  flex-direction: column;
  gap: .5rem;
`
